import React from "react";

const ExperienciaRoot = ({ children }: { children: React.ReactNode }) => {
  return <section className="flex flex-col gap-1">{children}</section>;
};

export default ExperienciaRoot;
